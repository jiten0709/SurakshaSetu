# Dummy local values, identical to the defaults in compose.yaml. To override, copy this file to
# infra/.env (gitignored) and edit. Keep them URL-safe: minio-init embeds the MinIO credentials in a URL.
POSTGRES_PASSWORD=surakshasetu-dev
MINIO_ROOT_USER=surakshasetu
MINIO_ROOT_PASSWORD=surakshasetu-dev-minio
